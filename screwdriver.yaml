cache:
    event:
        - $SD_SOURCE_DIR/node_modules
shared:
    image: node:6
jobs:
    job1:
        steps:
            - env: env
            - sleep: sleep 1000
            - install: npm install
        requires: [~commit, ~pr]
    job2-cache:
        steps:
            - ls: ls
            - install: npm install
        requires: [job1]
